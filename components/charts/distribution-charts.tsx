"use client";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

type Rating={question_id:string;prompt:string;average:number|null;distribution:Record<string,number>|null};
type Choice={question_id:string;prompt:string;distribution:Record<string,number>|null};
function ChartFrame({title,subtitle,children}:{title:string;subtitle?:string;children:React.ReactNode}){return <div className="rounded-2xl border bg-white p-5"><div><h3 className="font-bold">{title}</h3>{subtitle&&<p className="mt-1 text-sm text-[#71807a]">{subtitle}</p>}</div><div className="mt-5 h-64">{children}</div></div>}
export function DistributionCharts({ratings,choices}:{ratings:Rating[];choices:Choice[]}){return <div className="grid gap-5 xl:grid-cols-2">{ratings.map(item=><ChartFrame key={item.question_id} title={item.prompt} subtitle={`Average: ${item.average??"—"} / 5`}><ResponsiveContainer width="100%" height="100%"><BarChart data={Object.entries(item.distribution??{}).map(([label,value])=>({label,value}))}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="label"/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="value" fill="#267865" radius={[7,7,0,0]}/></BarChart></ResponsiveContainer></ChartFrame>)}{choices.map(item=><ChartFrame key={item.question_id} title={item.prompt}><ResponsiveContainer width="100%" height="100%"><BarChart layout="vertical" data={Object.entries(item.distribution??{}).map(([label,value])=>({label,value}))} margin={{left:20}}><CartesianGrid strokeDasharray="3 3" horizontal={false}/><XAxis type="number" allowDecimals={false}/><YAxis type="category" dataKey="label" width={90} tick={{fontSize:11}}/><Tooltip/><Bar dataKey="value" fill="#5d77b8" radius={[0,7,7,0]}/></BarChart></ResponsiveContainer></ChartFrame>)}</div>}
